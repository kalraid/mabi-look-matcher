# [Look Matcher] MVP — Custom Shell + Catalog + Gemini Match Hint

**Labels:** `ready-for-agent`

> GitHub 이슈 생성 시 제목·본문으로 사용. 본문 전체는 `docs/PRD-look-matcher.md`와 동일.

```bash
# 저장소·gh 로그인 후 예시
gh issue create --title "[Look Matcher] MVP — Custom Shell + Catalog + Gemini Match Hint" \
  --label "ready-for-agent" \
  --body-file docs/PRD-look-matcher.md
```

현재 `C:\devee\SOURCE\temp`는 git/gh 미설정 상태입니다.
